import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00D98A",
    },
    secondary: {
      main: "#00465E",
    },
  },
});

export default theme;
